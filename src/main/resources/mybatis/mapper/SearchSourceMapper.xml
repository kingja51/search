<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.search.mapper.SearchSourceMapper">

    <sql id="sourceColumns">
        s.doc_type, s.doc_id, s.title, s.body, s.link_url, s.category, s.updated_at, s.content_hash
    </sql>

    <!-- 해시 diff: 색인에 없거나 원문 해시가 달라진 문서만 (DESIGN.md 4.4 - 1단계) -->
    <select id="findChanged" resultType="SearchSource">
        SELECT <include refid="sourceColumns"/>
        FROM vw_search_source s
        LEFT JOIN tn_search_index i
               ON i.doc_type = s.doc_type AND i.doc_id = s.doc_id
        WHERE i.doc_id IS NULL OR i.content_hash &lt;&gt; s.content_hash
    </select>

    <!-- 전체 재색인용: 해시 비교 없이 전량 -->
    <select id="findAll" resultType="SearchSource">
        SELECT <include refid="sourceColumns"/>
        FROM vw_search_source s
    </select>

</mapper>
